import { ContactReservationView } from '../components/contact/ContactReservationView'

/** Same reservation flow as Contact; dedicated URL for navbar CTA / bookmarks. */
export function ReservationsPage() {
  return <ContactReservationView />
}
